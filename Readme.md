# Task

Create a QT application that would allow clients to create some arbitrary records on the server (clients and server are just separate windows). Clients should write requests to a queue and server should read from that queue and display received messages.

# Requirements

* C++17 or newer
* Have `qt` installed
* Have `make` installed

# Quick Start

* Run `make run` to generate, build, and execute the app

